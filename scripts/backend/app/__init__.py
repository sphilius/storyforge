# Director Mode Backend Package
# This __init__.py makes the backend/app/ directory a Python package,
# which allows the relative imports in our modules to work.
# (e.g., "from .config import ..." in adk_director.py)
